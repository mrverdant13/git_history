import 'package:flutter/material.dart';

class GitCommitHistoryScreen extends StatelessWidget {
  const GitCommitHistoryScreen();

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('Git Commit History'),
        ),
      );
}
